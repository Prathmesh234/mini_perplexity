[project]
name = "embedding"
version = "0.1.0"
description = "Simple embedding service using vLLM"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vllm>=0.8.5",
    "torch",
    "fastapi",
    "uvicorn",
    "pydantic",
]
