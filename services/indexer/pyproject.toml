[project]
name = "indexer"
version = "0.1.0"
description = "Indexer service for chunking and ingesting documents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "azure-storage-blob>=12.27.1",
    "azure-servicebus>=7.11.0",
    "python-dotenv>=1.0.1",
    "tqdm>=4.66.2",
]
